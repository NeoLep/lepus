# Lepus 产品功能说明

## 1. 产品定位

Lepus 是一个本地优先、面向实际工作的桌面 AI Agent。它将兼容 OpenAI API 的模型、文件与网页工具、任务规划、Skills 和权限控制整合在一个 Electron 应用中，让用户不只“与模型聊天”，还可以让 AI 在指定工作目录内完成可追踪、可确认的工作。

Lepus 适合需要在本地文件与在线资料之间协作的开发者、研究人员和知识工作者。模型服务可由用户自行配置，工作目录、工具权限和高风险操作由用户掌控。

## 2. 主要功能

### 2.1 可配置的模型接入

- 添加和切换多个模型配置。
- 自定义服务名称、Base URL、模型名称与 API Key。
- 兼容采用 OpenAI Chat Completions 接口的服务。
- 自动识别常见模型的上下文窗口，也允许手动设置上下文和最大输出 Token。
- 长对话接近上下文上限时自动压缩历史，并在界面中保留压缩记录。

### 2.2 Agent 与工具执行

Lepus 的模型可以根据任务选择工具，并在界面中展示工具名称、参数、进度、结果和状态。内置能力包括：

- 文件与目录检索、文本搜索、按行读取、精确补丁修改和文件写入。
- 文件复制、移动、创建目录，以及移动到系统废纸篓的安全删除。
- 文件类型、文件头、扩展名与 SHA-256 检查。
- 从公开 HTTPS 地址流式下载文件，显示下载进度并限制文件大小。
- 网页搜索、时间查询、算术计算和 UUID 生成。
- 在复杂任务中更新执行计划、向用户请求关键输入。

文件操作以用户选定的工作目录为边界，并对符号链接、路径越界、覆盖和危险删除做额外限制。

### 2.3 任务模式与子 Agent

任务模式有自动、开启和关闭三种状态。自动模式会结合请求是否包含复杂动作、多步骤要求、技术上下文或附件来判断是否启动任务工作流。

启动后，Lepus 可以：

- 创建并实时更新任务计划。
- 将相互独立的分析工作委派给受控子 Agent。
- 同时运行最多 2 个子任务，每轮最多委派 4 个任务。
- 在主界面展示子任务的排队、运行、等待批准、完成、失败或取消状态。
- 限制子 Agent 为只读模式，避免并行分析意外修改文件。

### 2.4 Skills 专业技能

Skills 用于给 Agent 注入某一类任务的专用说明、参考资料、脚本与资源。

- 在应用内创建、启用、停用和删除手动 Skill。
- 从本地目录或 GitHub 仓库导入包含 `SKILL.md` 的技能包。
- 为 Skill 配置触发词，也可在输入框使用 `/` 命令显式选择。
- 每次请求最多选择 3 个 Skill，并在回答区域显示当前启用项。
- Skill 的参考文件只能从导入清单中读取；第三方脚本在隔离的临时副本中运行，并要求用户确认。

### 2.5 多源网页搜索与引用

Lepus 支持配置多个网页搜索服务：

- Brave Search
- Tavily
- Exa
- Perplexity
- Firecrawl
- 自托管 SearXNG

只有已启用并完成配置的服务才会提供给 Agent。搜索结果包含标题、链接、摘要和可用的发布时间；回答中的引用可点击查看来源。

### 2.6 文件与多模态输入

- 支持 PNG、JPEG、GIF、WebP、PDF 和文本文件。
- 图片以多模态内容发送给支持视觉的模型。
- PDF 最多提取前 40 页文本，文本提取上限为 100,000 字符。
- 单次最多添加 10 个附件，总大小不超过 50 MiB。
- 附件复制到应用的受管目录，读取前会复核元数据和文件状态。

当前单文件限制：图片 20 MiB、PDF 30 MiB、文本 2 MiB。

### 2.7 权限与安全

每个会话可以单独设置工作目录和权限模式：

- **每次询问**：敏感工具执行前显示风险、原因和参数，由用户单次允许、会话允许或拒绝。
- **自动批准**：减少重复确认，同时保留工作目录与工具层的安全限制。
- **完全访问**：适合明确了解风险的高级用户。

下载、文件检查、删除和 Skill 脚本等高风险动作带有明确的批准流程；正在执行的下载或脚本可以取消。

### 2.8 会话与个性化

- 会话自动命名、全文搜索、重命名、置顶、归档、恢复和删除。
- 将会话导出为 Markdown 或 JSON。
- 修改用户消息后重新生成，或直接重新生成回答。
- 自定义系统指令，以及是否向模型提供时间、时区、语言和操作系统信息。
- 中英文界面、浅色/深色主题和可调整宽度的侧边栏。
- Markdown、代码块、表格、引用与来源提示渲染。

## 3. 典型工作流

1. 在“模型管理”中添加一个兼容 OpenAI API 的模型服务并设为当前模型。
2. 新建会话，在“文件与权限”中选择工作目录和权限模式。
3. 按需添加图片、PDF 或文本附件，并使用 `/` 选择专业 Skill。
4. 直接描述目标；复杂请求可使用自动或开启的任务模式。
5. 查看计划、工具调用、子任务和来源引用，在敏感动作出现时作出确认。
6. 完成后将会话置顶、归档，或导出为 Markdown/JSON。

## 4. 数据与隐私边界

- 会话、配置与附件由桌面应用在本地管理。
- 模型请求会发送到用户配置的模型服务；网络搜索会发送到用户启用的搜索服务。
- Lepus 不替用户提供模型或搜索服务账号，API Key 由用户自行配置。
- 是否适合处理敏感数据取决于用户所选模型服务、搜索服务和权限配置。

## 5. 技术形态

Lepus 基于 Electron、Vue 3 和 TypeScript 构建，使用 SQLite 保存本地数据，并通过 electron-vite 与 electron-builder 完成开发和跨平台打包。
